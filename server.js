/**
 * ESTE ARCHIVO HA SIDO MIGRADO A TYPESCRIPT.
 * El entry point ahora es: src/server.ts
 *
 * Para ejecutar:
 *   npm run dev   → ts-node-dev src/server.ts
 *   npm run build → tsc (compila a dist/)
 *   npm start     → node dist/server.js
 */
